// Public configuration only. Never put the API admin secret, session secret, password hash,
// peppers, Stripe credentials, or any other secret in this file.
window.SOLVELANG_ADMIN_GATEWAY_BASE = `${window.location.origin}/admin-gateway`;
