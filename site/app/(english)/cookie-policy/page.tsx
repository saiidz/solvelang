import type { Metadata } from "next";
import Link from "next/link";
export const metadata: Metadata = {"title": "Cookie policy", "alternates": {"canonical": "https://www.solve-lang.com/cookie-policy/"}};
const sections = [["Storage used for requested features", "SolveLang uses browser storage for selected language preferences, saved Studio projects and settings, and temporary purchase or report recovery. These functions support the service you choose to use. Local storage persists until removed; session storage normally ends with the browser tab or session."], ["Analytics and advertising", "This website does not load advertising pixels or third-party web analytics on this website. Studio diagnostic counters stay in memory and reset when the Studio instance is recreated. We do not request location lookup data to suggest a language; suggestions use browser language preferences."], ["Security and payment services", "Cloudflare security checks, account sign-in and Stripe checkout may use cookies or similar technology where those functions are requested. Their own privacy notices explain their processing. Links to GitHub or other websites open a separate service."], ["Cookie consent", "The current configuration uses storage for requested functions and does not enable optional tracking. No accept-all tracking banner is used for this configuration. You do not need to accept optional tracking to use these features."], ["Control browser data", "You can remove site data in your browser settings. Removing projects or recovery data can remove unsaved work or interrupt purchase recovery, so export work you need first. Contact hello@solve-lang.com about privacy choices."]];
export default function PolicyPage() { return <main className="min-h-screen bg-white px-6 py-14 text-slate-900"><article className="mx-auto max-w-3xl"><h1 className="text-4xl font-semibold">Cookie policy</h1>{sections.map(([heading,body]) => <section key={heading} className="mt-8"><h2 className="text-2xl font-semibold">{heading}</h2><p className="mt-3 leading-8">{body}</p></section>)}<p className="mt-8"><Link className="text-blue-800 underline" href="/preflight-privacy/">Workflow Preflight privacy details</Link></p></article></main>; }
