import type { ReactNode } from "react";
import AutomatedSupportTriageDemo from "./AutomatedSupportTriageDemo";

export default function SupportTriageLayout({ children: _children }: { children: ReactNode }) {
  return <AutomatedSupportTriageDemo />;
}
