import type { Metadata } from "next";
export const metadata: Metadata = { title: "Workflow Preflight Privacy — SolveLang", description: "Privacy and data handling for SolveLang Workflow Preflight." };
export default function Page(){return <main className="min-h-screen bg-white text-slate-900"><section className="mx-auto max-w-3xl px-6 py-20"><h1 className="text-4xl font-semibold">Workflow Preflight privacy</h1><div className="mt-8 space-y-6 leading-7 text-slate-600"><p>The initial workflow scan runs in the browser. The selected JSON file is not uploaded by Workflow Preflight v1, and the scanner does not execute nodes or call workflow services.</p><p>Checkout sends an opaque random scan ID to SolveLang&apos;s entitlement API and Stripe. Workflow names, node parameters, credential metadata, and report contents are not included in checkout metadata.</p><p>The browser may temporarily store the generated report and scan ID in session storage so a successful Stripe redirect can restore the purchase. Session storage is scoped to the browser tab and is removed after successful entitlement verification.</p><p>The entitlement service stores the opaque scan ID, Stripe Checkout Session ID, payment status, event ID, timestamps, and automatic expiration time. DynamoDB time-to-live removes expired entitlement records.</p><p>Conversion events are restricted to a fixed allowlist of event names. They do not include workflow content, filenames, report findings, credentials, email addresses, or customer-entered metadata.</p><p>Never upload workflow exports containing plaintext secrets. Rotate any secret that appears directly in an exported workflow.</p></div></section></main>}
