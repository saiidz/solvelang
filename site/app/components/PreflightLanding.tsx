import Link from "next/link";

export function PreflightLanding({ eyebrow, title, description, bullets }: { eyebrow: string; title: string; description: string; bullets: string[] }) {
  return <main className="min-h-screen bg-white text-slate-900"><section className="mx-auto max-w-5xl px-6 py-24 lg:px-8"><p className="text-sm font-semibold uppercase tracking-[0.2em] text-blue-700">{eyebrow}</p><h1 className="mt-5 text-4xl font-semibold tracking-tight sm:text-6xl">{title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-slate-600">{description}</p><ul className="mt-10 grid gap-4 sm:grid-cols-2">{bullets.map((bullet) => <li key={bullet} className="rounded-2xl border border-slate-200 bg-slate-50 p-5 text-slate-700">✓ {bullet}</li>)}</ul><div className="mt-10 flex flex-wrap gap-4"><Link href="/check/" className="rounded-xl bg-slate-950 px-6 py-3 text-sm font-semibold text-white">Scan an n8n workflow</Link><Link href="/support/" className="rounded-xl border border-slate-300 px-6 py-3 text-sm font-semibold">Read scanner help</Link></div><p className="mt-8 text-sm leading-6 text-slate-500">The initial scan runs locally in your browser. SolveLang does not execute the workflow or inspect credential values.</p></section></main>;
}
