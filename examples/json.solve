let text = "{\"name\":\"SolveLang\",\"features\":[\"json\",\"http\",\"files\"]}"
let data = json_parse(text)

print(data.name)
print(data.features[0])
print(json_stringify({ ok: true, name: data.name }))
