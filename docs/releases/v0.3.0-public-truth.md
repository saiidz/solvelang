# Public v0.3.0 truth

Safe current claims:

- SolveLang MCP Server v0.3.0 has a published GitHub Release.
- The repository's trusted npm publication workflow completed successfully for v0.3.0.
- The canonical plugin configuration launches `@solvelang/mcp-server@0.3.0`.
- The repository contains a Codex marketplace contract for managed-workspace import.

Claims still requiring external evidence:

- global public Plugin Directory availability;
- a particular managed workspace's successful import/install/use;
- independent blinded benchmark results;
- provider-token savings or provider-measured comparative performance;
- activation of protected production features that remain disabled.
