# If the Codex workspace import fails

Capture the exact non-sensitive failure evidence. Classify it as one of:

- repository manifest/schema/path defect;
- released package/MCP startup defect;
- workspace policy/eligibility/admin gate;
- platform/publication gate.

Only the first two return to repository fix → CI → merge. The latter two remain external until the responsible workspace/platform condition changes.
