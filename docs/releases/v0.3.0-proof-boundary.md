# v0.3.0 proof boundary

Repository evidence supports these statements:

- the v0.3.0 GitHub Release is published;
- the trusted npm publication workflow completed successfully, including its public-package publish step;
- the release package passed the workflow's MCP tests and packed clean-consumer `npx` verification;
- the canonical plugin points to v0.3.0;
- required post-merge checks are green.

Repository evidence alone does not support these statements:

- SolveLang is globally listed in the public Plugin Directory;
- an arbitrary or specific managed Codex workspace has imported/installed SolveLang;
- a genuinely blinded independent benchmark has completed;
- a provider-token savings percentage has been measured;
- disabled protected production features are live.
