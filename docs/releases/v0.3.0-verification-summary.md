# v0.3.0 verification summary

Release commit: `7d626c4303eba8c81141e94bf446cb89d8612864`

Verified repository evidence:

- GitHub Release `v0.3.0` exists and is published.
- `packages/mcp-server/package.json` is version `0.3.0`.
- canonical plugin `.mcp.json` invokes `@solvelang/mcp-server@0.3.0`.
- release-triggered **Publish MCP package** run `35177120485` completed successfully.
- that run completed MCP tests, packed clean-consumer install/`npx` verification, and the public package publish step.
- the release commit passed the four checks required by the active `Protect main` ruleset.
- the active ruleset requires PRs, conversation resolution and strict up-to-date required checks and has no bypass actors.

External evidence still required:

- actual managed-workspace Codex marketplace import/install/use;
- global public Plugin Directory listing/discovery;
- genuinely independent held-out evaluation;
- real provider measurements requiring provider execution/credits;
- separately authorized protected production activations.
