# Codex onboarding proof note

Two proof layers must remain distinct:

1. **Package/install-boundary proof:** repository release CI has already verified a packed clean-consumer install and `npx` entrypoint for v0.3.0.
2. **Codex workspace proof:** an eligible managed workspace administrator still has to import the GitHub marketplace, install SolveLang in Codex, and demonstrate fresh-session MCP/tool use.

The first does not substitute for the second.
