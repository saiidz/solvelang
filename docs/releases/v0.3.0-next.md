# After v0.3.0

Repository/publication work for the v0.3.0 package line is complete. Continue with evidence rather than another package change unless a concrete defect is found.

Next gates:

1. eligible managed-workspace administrator imports the SolveLang GitHub marketplace and records Codex install/use proof;
2. pursue the separate global public Plugin Directory publication/review/listing path;
3. independent evaluator executes the held-out protocol;
4. when separately authorized and credits are available, execute real Claude/Codex baseline-vs-context measurements;
5. separately authorize and verify any protected production activation tracked by #113.
