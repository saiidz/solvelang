# v0.3.0 external gates

The released repository/package line is complete enough that the remaining evidence below cannot be established by ordinary repository CI alone.

1. **Codex managed-workspace import/install/use** — requires an eligible workspace and administrator action.
2. **Global public Plugin Directory discovery** — requires the applicable OpenAI-controlled publication/review/listing path.
3. **Genuinely independent held-out Solve Context execution** — requires an evaluator/process independent of implementation authors and the repository's synthetic CI proof.
4. **Real Claude/Codex provider measurements** — require separately authorized provider execution and available credits.
5. **Protected production activations tracked by #113** — require their own exact-scope authorization, credentials/resources, current live preflight, monitoring and recovery proof.

Do not convert any of these pending gates into a positive availability, benchmark, savings, or production claim until its actual evidence is recorded.
