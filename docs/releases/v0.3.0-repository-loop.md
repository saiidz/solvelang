# Repository continuation rule after v0.3.0

Continue the engineering loop only when a concrete repository defect or no-credit launch gap is identified:

`fix → proportionate local/repository validation → protected PR CI → merge when green → post-merge verification`

Do not create code churn solely to keep the loop active after repository gaps are exhausted. External admin/evaluator/provider/production gates should remain explicit external gates until their prerequisites are available.
