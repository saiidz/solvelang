# Post-merge plan

After this PR merges and the merged head is green:

1. update #898 with the actual v0.3.0 release/npm evidence and remaining Solve Context external gates;
2. update #113 with completed main-protection governance and v0.3.0 publication truth while preserving protected production boundaries;
3. request only the external managed-workspace Codex proof from the owner/admin;
4. if that proof exposes a repository defect, resume fix → CI → merge; otherwise the no-credit repository loop is at its external-gate stop condition.
