# SolveLang MCP Server v0.3.0

_Status: released and published. The exact v0.3.0 distribution transition merged through #931 as `7d626c4303eba8c81141e94bf446cb89d8612864`. GitHub Release `v0.3.0` was published on 2026-09-17 and the release-triggered `Publish MCP package` workflow completed successfully._

## Release evidence

- Release commit: `7d626c4303eba8c81141e94bf446cb89d8612864` (#931).
- GitHub Release: `v0.3.0`, published 2026-09-17.
- npm release workflow: `Publish MCP package` run `35177120485`, conclusion `success`.
- The release job successfully verified tag/package version agreement, ran MCP tests, verified a packed clean-consumer install and `npx` entrypoint, and completed the public package publication step.
- The live `Protect main` ruleset is active with no bypass actors and requires pull requests, resolved conversations, up-to-date branches, and exactly `Rust runtime`, `Static site`, `SolveLang Rust security and tests`, and `WASM artifact security`.

## v0.3.0 capabilities

Compared with v0.2.0, v0.3.0 includes:

- deterministic Solve Context planning, packing, exact retrieval, SHA-256 provenance, content-addressed handles, and stale-source rejection;
- Claude ↔ Codex context handoff creation and freshness validation;
- correctness-first structured JSON/log/diff compaction with exact expansion;
- changed-path priority plus bounded supplied-Solve-Graph dependency/dependent/test evidence;
- expanded deterministic Solve Graph search, impact, path, cycle, hotspot, entrypoint, security-summary, and affected-validation surfaces;
- pinned first-party and independent external-source Solve Context regressions;
- strict offline baseline-vs-context evaluation records and pair-comparability gates;
- the #924 precommitted held-out evaluation protocol;
- locked MCP dependency security checks and packed-consumer/plugin roundtrip qualification; and
- the Codex GitHub-marketplace repository contract.

## Codex managed-workspace onboarding

Repository-side Codex onboarding is qualified, but a real managed-workspace installation still requires an eligible workspace administrator.

Use these exact import coordinates:

- GitHub source: `https://github.com/saiidz/solvelang`
- Path: leave blank / repository root
- Marketplace manifest: `.agents/plugins/marketplace.json`
- Plugin: `solvelang`
- Local plugin source: `./plugins/solvelang`
- Expected MCP package line: `@solvelang/mcp-server@0.3.0`

After import, verify that `solvelang` is visible/enabled for Codex, a clean Codex session discovers the SolveLang MCP surface on v0.3.0, and one read-only SolveLang operation succeeds. Record only non-sensitive evidence. A workspace import does not imply global public Plugin Directory publication.

## Remaining external gates

The repository/package release does not establish:

- a real managed-workspace Codex import/install/use result;
- global public Plugin Directory discovery;
- a genuinely independent held-out benchmark;
- provider-reported Claude/Codex token or latency measurements; or
- activation of SolveLang billing, paid priority/provider execution, connected support, analytics, customer-auth canaries, restore drills, or other protected production features.

Those remain external or separately authorized gates tracked by #898 and #113.

## Evidence boundary

Repository qualification and package publication do not establish provider-token savings, Claude/Codex superiority, production latency improvement, or public comparative advantage. Keep provider-reported, local-tokenizer, estimated, synthetic, and unavailable evidence distinct.

The visible-annotation external regression suite covers 4 repositories, 22 complete pinned source files, and 11 fixed tasks across Axios, Chalk, node-fetch, and Preact. The #924 held-out protocol machinery exists, but genuine independent execution still requires an external evaluator/process.

## Roll-forward policy

Published npm versions are immutable release artifacts. Any corrective package change after v0.3.0 should use a new version rather than rewriting v0.3.0. Production/customer state is separate from this package release and remains governed by its own authorization boundaries.
