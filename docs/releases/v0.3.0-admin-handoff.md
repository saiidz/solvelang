# Codex administrator handoff

Please import the SolveLang marketplace into an eligible managed Codex workspace using:

- Repository: `https://github.com/saiidz/solvelang`
- Path: blank / repository root
- Manifest discovered from root: `.agents/plugins/marketplace.json`
- Plugin: `solvelang` / SolveLang

Then install/enable SolveLang for a test user, start a fresh Codex session, confirm the MCP server starts and tools are discoverable, and exercise one read-only SolveLang tool.

Return only non-sensitive evidence: date, workspace scope/name if safe to disclose, whether import/install/start/tool-use succeeded, and screenshots with credentials/tokens/customer data excluded.

This proof is for the managed-workspace path only and must not be represented as global public Plugin Directory publication.
