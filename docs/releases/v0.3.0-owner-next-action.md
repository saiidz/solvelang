# Owner next action for v0.3.0

The first remaining action that repository automation cannot complete is the **Codex managed-workspace marketplace import/install proof**.

Use the coordinates and checklist in `../integrations/codex-install-evidence.md`. Once non-sensitive evidence is returned, record it in the repository and synchronize #898/#113.

No provider credits are needed for this workspace import/install proof itself; provider-backed benchmark work remains a separate later gate.
