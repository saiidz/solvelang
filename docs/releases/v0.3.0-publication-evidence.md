# SolveLang MCP v0.3.0 publication evidence

Recorded: 2026-09-17

## Released commit

- `main`: `7d626c4303eba8c81141e94bf446cb89d8612864`
- merge: `release(mcp): finalize v0.3.0 distribution (#931)`
- package: `@solvelang/mcp-server`
- checked-in package version: `0.3.0`
- canonical plugin `npx` pin: `@solvelang/mcp-server@0.3.0`

## GitHub release

The public GitHub Release `v0.3.0`, named `SolveLang MCP Server v0.3.0`, was published on 2026-09-17 and targets the release line containing the commit above.

## npm publication workflow evidence

GitHub Actions run `35177120485`, workflow **Publish MCP package**, completed successfully against exact head `7d626c4303eba8c81141e94bf446cb89d8612864`.

The recorded successful steps were:

1. checkout the released commit;
2. set up Node for npm trusted publishing;
3. install locked dependencies;
4. verify release tag matches package version;
5. run MCP tests;
6. verify packed consumer install and `npx` entrypoint;
7. publish public package.

The workflow's `npm-production` deployment/check also completed successfully.

This is repository-controlled evidence that the v0.3.0 trusted npm publication path completed. It must not be expanded into claims about a specific external npm registry response that was not independently captured.

## Post-merge verification

The exact release commit has successful required checks including:

- `Rust runtime`;
- `Static site`;
- `SolveLang Rust security and tests`;
- `WASM artifact security`.

The active `Protect main` ruleset requires pull requests, resolved review conversations, strict up-to-date status checks, the four checks above, deletion protection, and non-fast-forward protection, with no bypass actors.

## Codex install evidence boundary

Repository CI and the release workflow prove the package/plugin artifacts and clean-consumer `npx` path. They do **not** prove that an eligible managed Codex workspace has imported the GitHub marketplace or installed/search-discovered/used SolveLang there.

That evidence remains external and should record, at minimum:

- workspace marketplace import from `https://github.com/saiidz/solvelang` with blank/root path;
- SolveLang visible as an available Codex plugin;
- successful installation in a fresh Codex session;
- MCP startup/tool discovery using the released v0.3.0 pin.

Global public Plugin Directory discovery remains a separate OpenAI-controlled publication/review gate and must not be inferred from workspace import.
